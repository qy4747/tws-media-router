{
  "name": "tws-media-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('build',{recursive:true,force:true})\"",
    "build": "npm run clean && tsc",
    "start": "node build/index.js",
    "test": "npm run build && node --test"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.5.4"
  }
}
