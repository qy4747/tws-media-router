# Third-party notices

The NetEase CloudMusic elog decoding and detection approach in `src/detector/netease-play-state.ts` is adapted from [Coooookies/netease-cloudmusic-detector](https://github.com/Coooookies/netease-cloudmusic-detector).

The inspected local upstream checkout contains a top-level MIT `LICENSE` with copyright `2025 ButterCookies`; its `package.json` separately declares `ISC`. This checkpoint follows and reproduces the repository's top-level MIT license for the adapted source while recording the metadata mismatch rather than guessing that it has been resolved.

See `third_party/Coooookies-netease-cloudmusic-detector-LICENSE` for the reproduced upstream license text.

